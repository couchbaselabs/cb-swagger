{{#with ref ~}}
xref:{{dataType}}[]
{{~else if isModel ~}}
xref:{{dataType}}[]
{{~else if isMap ~}}
xref:{{dataType}}[]
{{~else if isAnyType ~}}
Any Type
{{~else ~}}
{{dataType}}{{#with dataFormat}} ({{{this}}}){{/with}}
{{~/with}}