{{#with ref ~}}
xref:{{dataType}}[]
{{~else if hasVars ~}}
{{#if isModel ~}}
xref:{{dataType}}[]
{{~else if isMap ~}}
xref:{{dataType}}[]
{{~/if}}{{! hasVars ~}}
{{~else if isAnyType ~}}
Any Type
{{~else ~}}
{{dataType}}{{#with dataFormat}} ({{{this}}}){{/with}}
{{~/with}}