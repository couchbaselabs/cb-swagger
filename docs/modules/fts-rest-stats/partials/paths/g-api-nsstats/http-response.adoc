A successful response returns an object like <<g-api-nsstats-ex-response-200>>, which contains statistics on 2 indexes, `travel-sample-index` and `color-test`.

If a user authenticates but does not have the appropriate permissions, the API returns a `403 Forbidden` response with an object similar to <<g-api-nsstats-ex-response-403>>.