<<g-api-stats-index-name-analyzeDoc-ex-response-200>> shows the result of analyzing the document from the <<g-api-stats-index-name-analyzeDoc-ex-request>> using a Search index with the following settings:

* A `keyword` analyzer for the `title` field.

* An `ngram` token filter with a `min` of 2 and a `max` of 5 for the `name` field.