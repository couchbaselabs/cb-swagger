The <<api-query-ex-curl-0>> request finds all active queries across the Search cluster.

The <<api-query-ex-curl-1>> request finds all queries across the cluster that have been running for longer than 7{nbsp}s.