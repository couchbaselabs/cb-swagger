The API schemes and host URLs are as follows:

* `pass:c[http://node:8093/]`
* `pass:c[https://node:18093/]` (for secure access)

where `node` is the host name or IP address of a computer running the Query service.