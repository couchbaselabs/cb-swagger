= Example HTTP Request

Get the `geo` subdocument from document `landmark_10019` and format the output, including metadata.

====
.Curl request
[source,sh]
----
include::example$get-subdoc.sh[tag=request]
----
====